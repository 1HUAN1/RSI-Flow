# 2026-09-21 评估修复后恢复

用户要求继续实验。原 BFCL 50 次失败请求完整归档到运行验证目录的
`failed_attempts/round_01_pre_evaluation_fix_20260921/`，未删除或改写旧成绩。

Task1、Meta1（7 条经验）、第一轮提交和第二轮提前完成的 180 条 rollout
均保留。原生 DurableExecutor 在启动前核对完成回执、Task 哈希、结果哈希、
checkpoint/Artifacts 和逐文件证据；没有调用新的 rollout 或 SFT。

新增 `completed_rollout_reuse.py` 是显式恢复授权接口，默认不启用。只有运行
目录内的 `recovery/completed_rollout_reuse.json` 指定了某个已完成父代批次、
精确控制器版本、旧协议和证据文件哈希时，才能复用。除实现/协议版本外，
模型、Harness、Artifacts、任务清单、seed 等执行条件必须相同；原 scope
与原始证据继续保留旧版本归属，不能伪装成新代码生成的轨迹。

该授权不是泛化的“所有旧结果都可用”：仅对本轮已审查的验证边界修复与
恢复接线成立。训练行为变化、未完成结果、不同 Task、篡改回执均不能复用。
普通新实验与未授权的断点仍使用原有严格一致性校验。

本次授权与旧协议保存在运行目录
`recovery/revisions/official_evaluation_20260921/`，包括源文件版本、原始
协议、复用授权和失败验证归档位置。恢复相关测试 **27 passed**。

使用原 `start_3round_training.sh` 与 `RSIFLOW_RESUME_DEPLOYED_RUN` 启动，
控制器先完成第一轮独立验证，通过后读取第二轮已有父代结果再进行 Meta
决策。此文档记录恢复动作，不表示 300 条验证或全部三轮已经完成。
