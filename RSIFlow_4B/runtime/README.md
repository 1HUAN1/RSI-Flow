# RSIFlow Task/Meta Runtime

This package contains the executable Task-Agent and Meta-Agent runtime used by
RSIFlow_4B. Task harness candidates are complete, content-addressed bundles
produced and validated through the pinned HarnessForge pipeline. Each round
evaluates one candidate on the same complete task set and deploys it only when
the measured success delta is strictly positive.
